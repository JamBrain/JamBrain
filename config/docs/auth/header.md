This is the **Authentication API** used by Starship (Ludum Dare). Go check out the [Public API](http://dev.ludumdare.com/api/) instead.

**IMPORTANT**: API compatibility is not guaranteed (not yet anyway).
