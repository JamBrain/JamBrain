import {h, Component}					from 'preact/preact';

export default class PageMyHome extends Component {
    render( /*props, state*/ ) {
        return (
            <div id="content">
                <div>Me/My/Self landing page goes here</div>
		<br />
		<div>Click the tabs above to browse (though only notifications work)</div>
            </div>
        );
    }
}
