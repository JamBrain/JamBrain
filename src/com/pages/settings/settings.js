import {h, Component} from 'preact/preact';

export default class PageSettings extends Component {
    render( props, state ) {
        let {user} = props;

        return (
            <div id="content">
                //content
            </div>
        );
    }
}
