import {h, Component}					from 'preact/preact';

export default class PageMySettings extends Component {
    render( /*props, state*/ ) {
        return (
            <div id="content">
                <strong>TODO:</strong> Settings
            </div>
        );
    }
}
